# Environment Variables for Render.com Deployment
# Copy these to your Render Web Service environment variables

# Django Configuration
DJANGO_ENV=production
SECRET_KEY=your-super-secret-django-key-here-make-it-long-and-random-at-least-50-characters

# Database Configuration
# Render will provide this automatically when you add PostgreSQL
DATABASE_URL=postgresql://username:password@hostname:port/database

# Email Configuration (Gmail SMTP example)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password-here
DEFAULT_FROM_EMAIL=ServicesBladi <your-email@gmail.com>

# Azure OpenAI Configuration (Optional - for chatbot functionality)
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here

# Render specific (automatically set by Render)
# RENDER=True
# RENDER_EXTERNAL_HOSTNAME=your-app-name.onrender.com
# PORT=10000

# Optional: Custom domain
# ALLOWED_HOST=yourdomain.com

# Optional: Redis URL (if you add Redis service)
# REDIS_URL=redis://username:password@hostname:port
